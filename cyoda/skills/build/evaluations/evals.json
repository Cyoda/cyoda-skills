{
  "skill_name": "build",
  "evals": [
    {
      "id": 1,
      "prompt": "/cyoda:build — add an Order entity with draft and submitted states",
      "expected_output": "Reads config, inspects existing models, generates workflow JSON, shows it for confirmation, and stops awaiting user approval — does not register without confirmation",
      "files": {
        ".cyoda/config": "{\"endpoint\": \"http://localhost:8080\", \"env\": \"development\"}"
      },
      "assertions": [
        { "id": "reads-config", "text": "Reads .cyoda/config for endpoint", "type": "behavior" },
        { "id": "inspects-models", "text": "Inspects existing entity models on the instance", "type": "behavior" },
        { "id": "asks-clarifying", "text": "Asks minimum necessary clarifying questions before generating — may proceed directly without asking when entity name, states, and transitions are fully specified in the request", "type": "behavior" },
        { "id": "shows-config-for-confirmation", "text": "Shows generated workflow JSON for user confirmation before registering", "type": "behavior" },
        { "id": "no-register-without-confirm", "text": "Does NOT register without showing config for confirmation first", "type": "behavior" }
      ]
    },
    {
      "id": 2,
      "prompt": "/cyoda:build — add a cancel transition to the Order entity",
      "expected_output": "Inspects existing Order entity, shows current states/transitions, asks which state 'cancel' transitions from, generates updated config, shows for confirmation, registers",
      "files": {
        ".cyoda/config": "{\"endpoint\": \"http://localhost:8080\", \"env\": \"development\"}"
      },
      "assertions": [
        { "id": "inspects-existing", "text": "Inspects existing Order entity models", "type": "behavior" },
        { "id": "shows-current-state", "text": "Shows current states and transitions before proposing changes", "type": "behavior" },
        { "id": "asks-source-state", "text": "Asks which state 'cancel' transitions from", "type": "behavior" },
        { "id": "shows-diff-for-confirm", "text": "Shows updated config for confirmation before registering", "type": "behavior" },
        { "id": "no-new-entity", "text": "Does NOT create a brand new entity instead of modifying existing", "type": "behavior" }
      ]
    },
    {
      "id": 3,
      "prompt": "/cyoda:build — add approval state to RiskAssessment",
      "expected_output": "Shows PRODUCTION warning at start, generates config normally, requires explicit 'confirm' before registering — does not block the full build loop",
      "files": {
        ".cyoda/config": "{\"endpoint\": \"https://api.eu.cyoda.net\", \"env\": \"production\", \"token\": \"eyJfake...\"}"
      },
      "assertions": [
        { "id": "shows-production-warning", "text": "Shows PRODUCTION warning at the start of the skill", "type": "behavior" },
        { "id": "generates-config-normally", "text": "Generates config and shows for confirmation as normal", "type": "behavior" },
        { "id": "requires-confirm-keyword", "text": "Requires user to type 'confirm' before registering to production", "type": "behavior" },
        { "id": "no-register-without-confirm", "text": "Does NOT register to production without explicit 'confirm'", "type": "behavior" },
        { "id": "no-full-block", "text": "Does NOT block the entire build loop — only adds the confirmation step", "type": "behavior" }
      ]
    }
    ,{
      "id": 4,
      "prompt": "/cyoda:build — register a new ChatRoom entity with a simple workflow",
      "expected_output": "Invokes cyoda:docs for any endpoint not listed in the skill, executes the correct sample-entity-then-workflow-import sequence, and does not guess or retry alternate URLs",
      "files": {
        ".cyoda/config": "{\"endpoint\": \"http://localhost:8080\", \"env\": \"development\"}"
      },
      "assertions": [
        { "id": "invokes-docs-for-unlisted-endpoints", "text": "Invokes cyoda:docs before calling any endpoint not explicitly listed in the skill", "type": "behavior" },
        { "id": "no-url-guessing", "text": "Does NOT construct or guess API URLs — uses only endpoints listed in the skill or confirmed via cyoda:docs", "type": "behavior" },
        { "id": "correct-sequence", "text": "Executes registration in the correct sequence: POST sample entity to /api/entity/JSON/{entity}/{version} first, then POST workflow to /api/model/{entity}/{version}/workflow/import", "type": "behavior" },
        { "id": "no-404-retry-loop", "text": "Does NOT retry failed endpoints with guessed variants — invokes cyoda:docs on 4xx before retrying", "type": "behavior" }
      ]
    }
    ,{
      "id": 5,
      "prompt": "/cyoda:build — register a new Order entity with draft and submitted states on a fresh instance",
      "expected_output": "Posts a sample entity to create the model in discover mode before importing the workflow — does not call workflow/import as the first API call",
      "files": {
        ".cyoda/config": "{\"endpoint\": \"http://localhost:8080\", \"env\": \"development\"}"
      },
      "assertions": [
        { "id": "entity-post-before-workflow", "text": "POSTs a sample entity to /api/entity/JSON/{entity}/1 before calling /api/model/{entity}/1/workflow/import", "type": "behavior" },
        { "id": "no-workflow-first", "text": "Does NOT call /api/model/{entity}/1/workflow/import as the first API call on a fresh instance", "type": "behavior" },
        { "id": "discover-mode-default", "text": "Does NOT lock the schema — stays in discover mode after workflow import", "type": "behavior" }
      ]
    }
    ,{
      "id": 6,
      "prompt": "/cyoda:build — I want to add a ChatRoom entity and a Message entity",
      "expected_output": "Builds both entities in discover mode without suggesting or performing schema locking — mentions discover mode stays active during development",
      "files": {
        ".cyoda/config": "{\"endpoint\": \"http://localhost:8080\", \"env\": \"development\"}"
      },
      "assertions": [
        { "id": "no-lock-suggestion", "text": "Does NOT suggest locking the schema during a normal development build session", "type": "behavior" },
        { "id": "no-lock-execution", "text": "Does NOT execute a schema lock operation", "type": "behavior" },
        { "id": "mentions-discover-mode", "text": "Mentions that schema stays in discover mode during development", "type": "behavior" }
      ]
    }
    ,{
      "id": 7,
      "prompt": "/cyoda:build — export the ChatRoom model schema so I can inspect it",
      "expected_output": "Invokes cyoda:docs to look up the export endpoint before making any curl call — does not guess the URL",
      "files": {
        ".cyoda/config": "{\"endpoint\": \"http://localhost:8080\", \"env\": \"development\"}"
      },
      "assertions": [
        { "id": "invokes-docs-for-export", "text": "Invokes cyoda:docs to look up the correct export endpoint path", "type": "behavior" },
        { "id": "no-url-guessing", "text": "Does NOT attempt curl calls with guessed URL variants (e.g. /api/model/export/JSON/SIMPLE_VIEW/... or /api/model/export/JSON/...)", "type": "behavior" },
        { "id": "no-options-call", "text": "Does NOT use curl OPTIONS for any diagnostic purpose", "type": "behavior" }
      ]
    }
    ,{
      "id": 8,
      "prompt": "/cyoda:build — add a Message entity with a sent state",
      "expected_output": "When a registration call returns 4xx, invokes cyoda:docs rather than retrying with guessed alternate paths",
      "files": {
        ".cyoda/config": "{\"endpoint\": \"http://localhost:8080\", \"env\": \"development\"}"
      },
      "assertions": [
        { "id": "invokes-docs-on-4xx", "text": "On a 4xx response, invokes cyoda:docs to look up the correct endpoint before retrying", "type": "behavior" },
        { "id": "no-guess-retry", "text": "Does NOT retry the call with guessed alternate URL variants after a 4xx", "type": "behavior" },
        { "id": "no-binary-search", "text": "Does NOT search the filesystem for the cyoda binary when cyoda is not in PATH", "type": "behavior" }
      ]
    }
    ,{
      "id": 9,
      "prompt": "/cyoda:build — add an archived state to the ChatRoom entity",
      "expected_output": "Detects ChatRoom already exists at version 1 and asks whether to update the existing version or create version 2 before generating any config",
      "files": {
        ".cyoda/config": "{\"endpoint\": \"http://localhost:8080\", \"env\": \"development\"}"
      },
      "assertions": [
        { "id": "reads-existing-version", "text": "Reads the current model version from the GET /api/model response", "type": "behavior" },
        { "id": "surfaces-version-decision", "text": "Asks user whether to update existing version 1 or create a new version 2", "type": "behavior" },
        { "id": "no-version-assumption", "text": "Does NOT silently assume version 1 and proceed without surfacing the decision", "type": "behavior" },
        { "id": "uses-resolved-version", "text": "Uses the version chosen by the user in all subsequent API calls", "type": "behavior" }
      ]
    }
    ,{
      "id": 10,
      "prompt": "/cyoda:build — yes, the Order entity config looks right, please register it",
      "expected_output": "Executes the registration POST calls (sample entity then workflow import), shows the API response, re-inspects the model state, reports completion, and suggests running /cyoda:test",
      "files": {
        ".cyoda/config": "{\"endpoint\": \"http://localhost:8080\", \"env\": \"development\"}"
      },
      "assertions": [
        { "id": "posts-sample-entity", "text": "Attempts POST to /api/entity/JSON/Order/{version} with a sample payload before the workflow import", "type": "behavior" },
        { "id": "posts-workflow-import", "text": "Attempts POST to /api/model/Order/{version}/workflow/import with the workflow config", "type": "behavior" },
        { "id": "shows-api-response", "text": "Shows the API response (success or failure) to the user rather than silently continuing", "type": "behavior" },
        { "id": "re-inspects-after-registration", "text": "Re-inspects the running instance (GET /api/model) after registration to show what changed", "type": "behavior" },
        { "id": "suggests-test", "text": "Offers to run /cyoda:test or suggests it as the next step", "type": "behavior" }
      ]
    },
    {
      "id": 11,
      "prompt": "/cyoda:build — add a Product entity",
      "expected_output": "Detects 401 on GET /api/model, invokes cyoda:auth to refresh, retries the model inspection once",
      "files": {
        ".cyoda/config": "{\"endpoint\": \"https://api.eu.cyoda.net\", \"env\": \"development\", \"token\": \"eyJexpired...\"}"
      },
      "assertions": [
        { "id": "invokes-auth-on-401", "text": "Invokes cyoda:auth when any API call returns 401 or 403", "type": "behavior" },
        { "id": "retries-once-after-refresh", "text": "Retries the failed call exactly once after auth refresh", "type": "behavior" },
        { "id": "no-retry-on-other-errors", "text": "Does NOT retry on 404 or 5xx — only on 401/403", "type": "behavior" }
      ]
    }
  ]
}
